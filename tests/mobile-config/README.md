```
# These two files can be compared using diff
# which is as follows from the project root:
node ./tests/sample-output.js | diff ./tests/mobile-config-expected.js -
# one file is run while the other is read as is
# this has been added to the list of tests in package.json
```
